[project]
name = "music-reader"
version = "0.1.0"
description = "Sheet music transcription using HOMR"
requires-python = ">=3.11"
dependencies = [
    "homr==0.7.0",
    "mido>=1.3.3",
    "pymupdf>=1.28.0",
    "verovio>=6.2.1",
]

[project.scripts]
music-reader = "main:main"
